import 'package:flutter/material.dart';

class RegisteredPersons extends StatelessWidget {
  const RegisteredPersons({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
