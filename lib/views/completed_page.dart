import 'package:flutter/material.dart';

class CompletedView extends StatelessWidget {
  const CompletedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("Completed"),
    );
  }
}
